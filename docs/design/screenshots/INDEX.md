# Screenshot index

All captured from the interactive prototype (`../prototype/DLS-CMS Prototype.dc.html`) — match these as closely as possible.

| File | Screen |
|---|---|
| 01 | Desktop · Admin · Dashboard (Agency overview, collapsible sidebar with sections — CORE expands by default) |
| 02 | Desktop · Admin · Schedule (staff × weekday grid; red chip = no physician order, cannot be saved) |
| 03 | Desktop · Admin · Billing (claim readiness with blockers, 837P export) |
| 04 | Desktop · Admin · Payroll transmittal (Wk1/Wk2 + OT, "all notes in?", certification) |
| 05 | Desktop · Admin · Settings & users (users, add user, menu configuration per role, permission matrix) |
| 06 | Desktop · Field Staff (impersonated) · My visits |
| 07 | Mobile · Admin · hamburger drawer open over dashboard (drawer mirrors desktop menu) |
| 08 | Mobile · Field Staff · Visit detail (Clock In, NMT trips, photos/documents → S3, client info) |
| 09 | Mobile · Field Staff · Progress note (live unit calc, goals, signatures below) |
| 10 | Mobile · Field Staff · Today home (default "Visits" style; switchable to Dashboard) |

Note: screenshots show the top-of-page viewport; scroll the live prototype for full-length screens (progress note, settings, etc.). The top bar (Desktop/Mobile, Impersonate, offline, reset) is a **demo harness**, not product UI — except Impersonate, which represents the Admin-only impersonation feature described in the README.
